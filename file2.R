# assign my file name
# initial command
# assign
x1 = 3 # press control+enter to run the line
x2 <- 3 # same
# which is better
x1
x2
y
ls() # variable in env
women
AirPassengers
data() #data set available for use

library() # library currently loaded

?mean #help
help(mean)
??mean
x=0:10
x
x <- c(0:10, 50)
x
xm <- mean(x)
c(xm, mean(x, trim = 0.10))

x <- c(0:10, 50)
x
xm <- mean(x)
xm
mean(x, trim = 01.)
c(mean(x), mean(x, trim = 0.10))

1:10
1:10000000
x=c(1,34,5)
x
?c


version # know version
sys.date()
getwd() # working directry
methods(class='matrix') # method available for a class of object

plot(1:100) #basic command for plt
plot(women)
